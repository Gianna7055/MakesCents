# Phase 2 Notes

Move paychecks to their own screen and have users order them based on what money should be populated first, second, third, etc.

Add a default account setting (and have transactions default to that account)
